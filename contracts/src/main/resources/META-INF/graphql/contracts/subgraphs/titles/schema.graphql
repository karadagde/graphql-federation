
type Query {
    title(id: ID!): Title
    titles: [Title!]!
}

type Title @key(fields: "id") {
    id: ID!
    name: String!
    year: Int!
}

extend schema
@link(url: "https://specs.apollo.dev/federation/v2.5", import: ["@key"])
